# Vagrant Lab

This repo will contain my experences with Vagrant

The first commit is about a simple machine created with Ubuntu
The machine will use the localhost:8080 to provide a little html page with bootstrap
This page will be served with Apache
